# Raw Higgsfield renders (keyframes, clips, master film) — kept locally, not needed to serve the site
_higgsfield/

# Local tooling
.claude/
.impeccable/hook.cache.json
node_modules/

# OS
.DS_Store
Thumbs.db
desktop.ini

*.patch
*.zip

# Private/local SEO follow-up checklist (contains owner-dependent details)
followup.txt
